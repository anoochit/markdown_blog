import 'package:flutter/material.dart';

import 'package:get/get.dart';

class SidebarView extends GetView {
  const SidebarView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('SidebarView');
  }
}
